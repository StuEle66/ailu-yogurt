# Ailu fork：项目协作规则

## 来源与产品边界

- 本仓库基于上游 `https://github.com/mcncarl/ailu`，个人仓库为 `https://github.com/StuEle66/ailu-yogurt`。上游远程名为 `upstream`，个人远程名为 `origin`；固定基线为 `8a232fe082163c5898038cca7bcf26cb1956b9a2`，功能开发使用独立分支。
- 插件 ID 和包名保持 `ailu`，显示名保持 `Ailu`，Vault 与用户目录的数据命名空间保持 `.ailu`。当前定制版本为 `0.5.5`；版本变更须同步 manifest、package、lockfile、versions、构建证明和发行校验器。
- 保留原作者信息、AGPL-3.0-or-later、第三方来源和许可证。定制构建不冒充上游官方发行版。
- 当前定制范围包括公众号正文图片复制、独立封面选择与裁剪，以及从固定 MDFlow 基线迁入的小红书本地图卡工作流。扩展范围前先更新规格与验收标准。

## 开发与验证

- 先核对真实源码、Git 状态与目标路径。不要覆盖其他人的未提交改动。
- 本项目的每项完整改动都必须使用 Git 管理。开始前检查工作树和当前分支；功能、修复、文档或构建调整完成后，按一个可解释的行为边界形成原子提交，不把无关变化混进同一提交。
- 提交信息使用清楚的类型和结果，例如 `feat: add WeChat cover crop workflow`、`fix: preserve managed preview images when copying`。提交前运行与改动相符的测试；准备交付时运行完整检查，并在交付说明中给出分支、提交号和验证结果。
- `main` 只保留已经验收、可构建的版本；日常开发从 `main` 创建 `feat/*`、`fix/*` 或 `docs/*` 分支。合并前检查与 `upstream` 的差异，解决冲突后重新验证。
- 完成的本地提交应推送到 `origin` 的对应分支作为备份。不得强制推送、改写已发布历史或删除远端分支，除非用户针对具体操作明确授权。
- 采用 TDD：在已确认的公开行为边界写一个有效失败测试，再实现到通过。依赖缺失、编译错误不能作为行为失败证据；只在外部系统边界使用替身。
- 保留已有测试。变更后执行 `npm run check` 和 `npm run audit:dependencies`；构建内含 TypeScript 检查、公开源码清单检查、构建证明和发行验证，不得绕过检查解决失败。
- 图片尺寸、裁剪与剪贴板使用真实浏览器或 Obsidian 验证，不能以模拟 DOM 的尺寸证明视觉正确。先在测试 Vault 验收，再部署日常 Vault。
- 新增公开源码须人工检查并登记在 `public-source-files.json`。仅根目录本文件可公开；嵌套 AGENTS、个人路径、凭据和运行数据仍禁止进入源码树。Git 索引必须与审核后的源码清单和内容一致。

## 数据与安装

- 不硬编码个人 Vault 路径；路径通过配置或调用参数传入。不要提交凭据、Provider 配置、会话、诊断日志、验收截图或其他运行数据；验收证据放在仓库外。
- 修改正文必须绑定发起操作的文章，校验目标，不能因切换文章而写错文件。取消不得写入原文；封面原图与旧附件不自动删除。
- 日常部署前完全退出 Obsidian，备份插件、配置和需要保留的数据，记录构建哈希与回退位置。使用现有部署工具及其校验，保留其他插件启用状态。
- 禁止自动删除或清空 `.ailu`、现有会话与设置。不要通过重建数据目录解决兼容问题。
- 用户已要求本项目后续改动持续使用 Git 管理，因此完成并验证的项目改动应提交并推送到 `origin`。发布 Obsidian 社区版本、创建 Release、向上游提交 PR 或写入其他外部平台，仍须用户针对该动作明确指令。

## 验收产物清理

- 验收完成且结果确认正确后，应清理可重建的中间产物，不在工作区长期堆积测试 Vault、临时浏览器 Profile、调试日志、失败截图、临时验收脚本和测试导出文件。
- 清理前先保存精简验收摘要、必要的最终截图、关键哈希、部署前备份位置和部署回退收据，并确认这些保留项真实存在且可读。
- 不得把源码、用户文章、唯一正式产物、`.ailu`、现有会话与设置、旧插件数据或回退备份当作验收中间产物删除；归属不明确时先停止清理并核实。
- 清理后核对保留文件、释放空间和 Git 状态；仓库内的规则或文档变化继续按本项目 Git 流程提交并推送。
